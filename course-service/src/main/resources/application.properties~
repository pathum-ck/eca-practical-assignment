# --- Application Name ---
spring.application.name=course-service

# --- Server Port ---
server.port=8081

# --- Database (SQL Server on Azure) ---
spring.datasource.url=jdbc:sqlserver://demoservereca.database.windows.net:1433;database=demoDB;encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30;
spring.datasource.username=pathumck
spring.datasource.password=aaa%IJSE
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# --- JPA / Hibernate ---
# Show SQL in the console
spring.jpa.show-sql=true
# Automatically update schema (good for dev; use validate/none in prod)
spring.jpa.hibernate.ddl-auto=update
# Hibernate dialect for Microsoft SQL Server
spring.jpa.database-platform=org.hibernate.dialect.SQLServerDialect
